import html from '../libs/html.js';

let template = function() {
    return html`
        
   `;
};

export default class {
    constructor(props) {
       
    }

    render() {
        let dom = template();
        return dom;
    }
}